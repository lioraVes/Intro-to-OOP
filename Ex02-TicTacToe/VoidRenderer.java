/**
 * VoidRenderer class- a renderer that doesn't visualise the board on the screen.
 */
public class VoidRenderer implements Renderer {
    /**
     * This method does nothing, since VoidRenderer doest visualise the board.
     *
     * @param board - the board
     */
    @Override
    public void renderBoard(Board board) {
    }
}
